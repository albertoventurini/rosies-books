/** Owns users, current-user resolution, authentication, and authorization. */
@AppModule(
    name = "identity",
    allowedDependencies = {"platform.api"})
package com.albertoventurini.rosiesbooks.identity;

import com.albertoventurini.rosiesbooks.AppModule;
