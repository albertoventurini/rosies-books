/**
 * Public contracts exposed by the identity feature.
 *
 * <p>{@code CurrentUser} is the owner value required at private-library boundaries. {@code
 * CurrentUserProvider} resolves it for the current request and returns empty when unauthenticated.
 */
package com.albertoventurini.rosiesbooks.identity.api;
